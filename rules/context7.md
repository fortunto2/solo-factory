# Context7 — Library Documentation

When working with external libraries, use Context7 MCP for up-to-date docs:

```
mcp__context7__resolve-library-id("query", "library_name")
mcp__context7__query-docs("/org/library", "how to do X")
```

Common libraries: `/hubspot/hubspot-api-nodejs`, `/openai/openai-python`, `/fastapi/fastapi`, `/pydantic/pydantic`, `/vercel/next.js`, `/supabase/supabase-js`.
